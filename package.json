{
  "name": "domain-intel-collector",
  "version": "1.0.0",
  "private": true,
  "description": "A local authorized domain, DNS, website, TLS, and limited port information collector.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
