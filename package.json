{
  "name": "file-upload-site",
  "version": "1.0.0",
  "private": true,
  "description": "A simple local website for uploading files.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
